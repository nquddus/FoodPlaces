<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.naeemquddus.foodplaces" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".main_menu"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".join_group_menu"
            android:label="@string/title_activity_join_group_menu" >
        </activity>
        <activity
            android:name=".create_group_menu"
            android:label="@string/title_activity_create_group_menu" >
        </activity>
        <activity
            android:name=".food_type"
            android:label="@string/title_activity_food_type" >
        </activity>
        <activity
            android:name=".end_result"
            android:label="@string/title_activity_end_result" >
        </activity>
    </application>

</manifest>
